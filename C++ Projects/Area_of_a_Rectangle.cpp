#include<iostream>
using namespace std;

int main(){
    // Calculate Area of a Rectangle.
        // 1) Using Hard Coded Inputs.
        // 2) Using Inputs Supplied by the User.
    int l, b;
    cout<<"Enter Length in Area of a Rectangle: ";
    cin>>l;
    
    cout<<"Enter Breadth in Area of a Rectangle: ";
    cin>>b;

    cout<<"\nArea of a Rectangle is: "<<(l*b)<<endl;
    return 0;
};